import CustomButton from '../../components/Button/Button'
import styles from './Start.module.scss'

const Start = () => {
  return (
    <div className={styles.start}>
      <CustomButton>Старт</CustomButton>
    </div>
  )
}

export default Start
